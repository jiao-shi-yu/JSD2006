Java 语言欠缺事件监听、多重继承等功能。

在面向对象编程中，需要把不同的框架结合起来使用，各个组件之间，需要用胶水连接起来。
一些共有的类，各个组件都会用到。对于这些共有类，各个组件之间约定了一套规范。
满足这套规范的类就叫 Java Bean。

这种规范，一般是:

1. 属性私有化
2. 提供默认构造方法
3. 提供 getter 和 setter 方法
4. 实现 serializable 接口







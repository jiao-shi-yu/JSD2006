# 5.2 Spring IoC
## 5.2 通过SET方式注入值
### 5.2.1 注入List集合类型
```
<list name="skills"><property><value>Java SE</value></property></list>
```


##### 关于List与Set集合
`List`与`Set`都是`Collection`的子接口。


`List`集合是序列的，并且集合中的元素可以重复，在内存中是连续的地址。
`Set`

### 5.2.2 注入Set集合类型
### 5.2.3 注入Map集合类型
世上本无SET， 先有了Map，后有了Set. Set看做Map的key部分。
其实，每一个`Set`都是一个不关心value的Map集合。

### 5.2.4 注入Array类型

### 5.2.5 注入Properties类型的值
通常关于项目的配置，都会写在专门的`.properties`文件中

### 5.3 有参数构造


# 6. Spring表达式

# 7. 自动装配



